<?xml version="1.0" encoding="utf-8"?>
<!--
   Board Card Item Layout
   Used in the recent boards horizontal list on the home screen.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_board"
    android:layout_width="180dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/spacing_md"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="@dimen/radius_md"
    app:cardElevation="@dimen/elevation_sm"
    app:rippleColor="@color/ripple_light"
    style="@style/Widget.TaskFlow.Card">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Color Header Bar -->
        <View
            android:id="@+id/view_color_bar"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:background="@color/primary"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/spacing_md">

            <!-- Icon and Name -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/text_icon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    tools:text="🚀"/>

                <TextView
                    android:id="@+id/text_name"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textAppearance="@style/TextAppearance.TaskFlow.Title.Small"
                    android:textColor="@color/text_primary"
                    tools:text="Product Launch"/>

            </LinearLayout>

            <!-- Description -->
            <TextView
                android:id="@+id/text_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xs"
                android:ellipsize="end"
                android:maxLines="2"
                android:textAppearance="@style/TextAppearance.TaskFlow.Body.Small"
                android:textColor="@color/text_secondary"
                tools:text="Q1 Product Launch - Mobile App Release"/>

            <!-- Task Count -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_md"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="@dimen/icon_sm"
                    android:layout_height="@dimen/icon_sm"
                    android:src="@drawable/ic_shortcut_tasks"
                    android:tint="@color/text_tertiary"
                    android:contentDescription="@null"/>

                <TextView
                    android:id="@+id/text_task_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/spacing_xs"
                    android:textAppearance="@style/TextAppearance.TaskFlow.Label.Small"
                    android:textColor="@color/text_tertiary"
                    tools:text="12 tasks"/>

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
