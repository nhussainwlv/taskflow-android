<?xml version="1.0" encoding="utf-8"?>
<!--
   Empty State Component
   Reusable layout for empty states across the app.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/spacing_xxl">

    <ImageView
        android:id="@+id/image_empty"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_empty_tasks"
        android:contentDescription="@null"/>

    <TextView
        android:id="@+id/text_empty_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_lg"
        android:textAppearance="@style/TextAppearance.TaskFlow.Title.Medium"
        android:textColor="@color/text_primary"
        tools:text="No tasks yet"/>

    <TextView
        android:id="@+id/text_empty_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_sm"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.TaskFlow.Body.Medium"
        android:textColor="@color/text_secondary"
        tools:text="Add a task to get started"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_empty_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_lg"
        android:visibility="gone"
        style="@style/Widget.TaskFlow.Button.Tonal"
        tools:text="Add Task"
        tools:visibility="visible"/>

</LinearLayout>
